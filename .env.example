# GPT-2 is a public model and needs NO credentials to download or run.
# This file is here only to keep the repo structure consistent and to show where a token
# would go if you later swap in a gated model (e.g. Llama) that requires Hugging Face auth.

# HF_TOKEN=your_huggingface_token_here
